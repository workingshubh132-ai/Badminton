[pytest]
markers =
    integration: loads the real person-detection model and/or calls the HTTP API — slower, but still fully deterministic and CI-safe (no network calls, no external model download at test time). Run everything with `pytest`, or skip these with `pytest -m "not integration"` for a fast inner-loop.
